{
  "name": "timeuse_tool",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "live-server --port=8080 --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "netlify-cli": "^19.0.2"
  }
}
