
{
  "name": "timeuse_tool",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "live-server --port=8080 --no-browser"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}